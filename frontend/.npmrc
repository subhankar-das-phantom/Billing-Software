# Prevent automatic installation of peer dependencies
legacy-peer-deps=false
strict-peer-dependencies=true

# Use exact versions for security-critical packages
save-exact=false
save-prefix=^

# Enable audit automatically
audit=true
