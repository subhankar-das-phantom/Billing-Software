/**
 * Centralized theme color constants for JS contexts
 * (charts, console logs, inline styles, hex values)
 *
 * To change the app's accent color, update these values
 * AND the @theme block in index.css
 */
export const THEME = {
  accent: {
    hex: '#14b8a6',      // teal-500
    hexLight: '#2dd4bf',  // teal-400
    rgb: '20, 184, 166',  // for rgba()
  },
  accent2: {
    hex: '#06b6d4',      // cyan-500
    hexLight: '#22d3ee',  // cyan-400
    rgb: '6, 182, 212',   // for rgba()
  },
};
